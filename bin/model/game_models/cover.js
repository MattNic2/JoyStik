class CoverModel {
    final int id;
    final int height;
    final int width;
    final String imageId;
    final String url;
    final int game;

    Covermodel(this.id, this.height, this.width, this.imageId, this.url);

    CoverModel.fromJson
}