import { DefaultTheme } from 'react-native-paper';

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    surface: '#272759',
    text: '#ffffff',
    primary: 'aliceblue',
    error: '#f13a59',
    placeholder: '#ffffff',
  },
}
